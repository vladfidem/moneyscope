import { FC } from 'react'
import { SafeAreaView } from 'react-native-safe-area-context'
import { AppText } from '../components'

export const SignUpScreen: FC = () => {
  return (
    <SafeAreaView>
      <AppText>Sign Up Screen</AppText>
    </SafeAreaView>
  )
}
