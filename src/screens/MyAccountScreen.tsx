import { FC } from 'react'
import { SafeAreaView } from 'react-native-safe-area-context'
import { AppText } from '../components'

export const MyAccountScreen: FC = () => {
  return (
    <SafeAreaView>
      <AppText>My Account Screen</AppText>
    </SafeAreaView>
  )
}
